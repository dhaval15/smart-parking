import 'package:flutter/material.dart';

class SmartBar extends PreferredSize {
  SmartBar()
      : super(
          child: Container(),
          preferredSize: Size(double.infinity, 64),
        );
}
